# generated from rosidl_generator_py/resource/_idl.py.em
# with input from igris_c_sdk:msg/LowCmd.idl
# generated code does not contain a copyright notice

# This is being done at the module level and not on the instance level to avoid looking
# for the same variable multiple times on each instance. This variable is not supposed to
# change during runtime so it makes sense to only look for it once.
from os import getenv

ros_python_check_fields = getenv('ROS_PYTHON_CHECK_FIELDS', default='')


# Import statements for member types

import builtins  # noqa: E402, I100

# Member 'kinematic_modes'
import numpy  # noqa: E402, I100

import rosidl_parser.definition  # noqa: E402, I100


class Metaclass_LowCmd(type):
    """Metaclass of message 'LowCmd'."""

    _CREATE_ROS_MESSAGE = None
    _CONVERT_FROM_PY = None
    _CONVERT_TO_PY = None
    _DESTROY_ROS_MESSAGE = None
    _TYPE_SUPPORT = None

    __constants = {
        'KINEMATIC_MODE_MS': 0,
        'KINEMATIC_MODE_PJS': 1,
    }

    @classmethod
    def __import_type_support__(cls):
        try:
            from rosidl_generator_py import import_type_support
            module = import_type_support('igris_c_sdk')
        except ImportError:
            import logging
            import traceback
            logger = logging.getLogger(
                'igris_c_sdk.msg.LowCmd')
            logger.debug(
                'Failed to import needed modules for type support:\n' +
                traceback.format_exc())
        else:
            cls._CREATE_ROS_MESSAGE = module.create_ros_message_msg__msg__low_cmd
            cls._CONVERT_FROM_PY = module.convert_from_py_msg__msg__low_cmd
            cls._CONVERT_TO_PY = module.convert_to_py_msg__msg__low_cmd
            cls._TYPE_SUPPORT = module.type_support_msg__msg__low_cmd
            cls._DESTROY_ROS_MESSAGE = module.destroy_ros_message_msg__msg__low_cmd

            from igris_c_sdk.msg import Header
            if Header.__class__._TYPE_SUPPORT is None:
                Header.__class__.__import_type_support__()

            from igris_c_sdk.msg import MotorCmd
            if MotorCmd.__class__._TYPE_SUPPORT is None:
                MotorCmd.__class__.__import_type_support__()

    @classmethod
    def __prepare__(cls, name, bases, **kwargs):
        # list constant names here so that they appear in the help text of
        # the message class under "Data and other attributes defined here:"
        # as well as populate each message instance
        return {
            'KINEMATIC_MODE_MS': cls.__constants['KINEMATIC_MODE_MS'],
            'KINEMATIC_MODE_PJS': cls.__constants['KINEMATIC_MODE_PJS'],
        }

    @property
    def KINEMATIC_MODE_MS(self):
        """Message constant 'KINEMATIC_MODE_MS'."""
        return Metaclass_LowCmd.__constants['KINEMATIC_MODE_MS']

    @property
    def KINEMATIC_MODE_PJS(self):
        """Message constant 'KINEMATIC_MODE_PJS'."""
        return Metaclass_LowCmd.__constants['KINEMATIC_MODE_PJS']


class LowCmd(metaclass=Metaclass_LowCmd):
    """
    Message class 'LowCmd'.

    Constants:
      KINEMATIC_MODE_MS
      KINEMATIC_MODE_PJS
    """

    __slots__ = [
        '_header',
        '_kinematic_modes',
        '_motors',
        '_check_fields',
    ]

    _fields_and_field_types = {
        'header': 'igris_c_sdk/Header',
        'kinematic_modes': 'uint8[5]',
        'motors': 'igris_c_sdk/MotorCmd[31]',
    }

    # This attribute is used to store an rosidl_parser.definition variable
    # related to the data type of each of the components the message.
    SLOT_TYPES = (
        rosidl_parser.definition.NamespacedType(['igris_c_sdk', 'msg'], 'Header'),  # noqa: E501
        rosidl_parser.definition.Array(rosidl_parser.definition.BasicType('uint8'), 5),  # noqa: E501
        rosidl_parser.definition.Array(rosidl_parser.definition.NamespacedType(['igris_c_sdk', 'msg'], 'MotorCmd'), 31),  # noqa: E501
    )

    def __init__(self, **kwargs):
        if 'check_fields' in kwargs:
            self._check_fields = kwargs['check_fields']
        else:
            self._check_fields = ros_python_check_fields == '1'
        if self._check_fields:
            assert all('_' + key in self.__slots__ for key in kwargs.keys()), \
                'Invalid arguments passed to constructor: %s' % \
                ', '.join(sorted(k for k in kwargs.keys() if '_' + k not in self.__slots__))
        from igris_c_sdk.msg import Header
        self.header = kwargs.get('header', Header())
        if 'kinematic_modes' not in kwargs:
            self.kinematic_modes = numpy.zeros(5, dtype=numpy.uint8)
        else:
            self.kinematic_modes = kwargs.get('kinematic_modes')
        from igris_c_sdk.msg import MotorCmd
        self.motors = kwargs.get(
            'motors',
            [MotorCmd() for x in range(31)]
        )

    def __repr__(self):
        typename = self.__class__.__module__.split('.')
        typename.pop()
        typename.append(self.__class__.__name__)
        args = []
        for s, t in zip(self.get_fields_and_field_types().keys(), self.SLOT_TYPES):
            field = getattr(self, s)
            fieldstr = repr(field)
            # We use Python array type for fields that can be directly stored
            # in them, and "normal" sequences for everything else.  If it is
            # a type that we store in an array, strip off the 'array' portion.
            if (
                isinstance(t, rosidl_parser.definition.AbstractSequence) and
                isinstance(t.value_type, rosidl_parser.definition.BasicType) and
                t.value_type.typename in ['float', 'double', 'int8', 'uint8', 'int16', 'uint16', 'int32', 'uint32', 'int64', 'uint64']
            ):
                if len(field) == 0:
                    fieldstr = '[]'
                else:
                    if self._check_fields:
                        assert fieldstr.startswith('array(')
                    prefix = "array('X', "
                    suffix = ')'
                    fieldstr = fieldstr[len(prefix):-len(suffix)]
            args.append(s + '=' + fieldstr)
        return '%s(%s)' % ('.'.join(typename), ', '.join(args))

    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        if self.header != other.header:
            return False
        if any(self.kinematic_modes != other.kinematic_modes):
            return False
        if self.motors != other.motors:
            return False
        return True

    @classmethod
    def get_fields_and_field_types(cls):
        from copy import copy
        return copy(cls._fields_and_field_types)

    @builtins.property
    def header(self):
        """Message field 'header'."""
        return self._header

    @header.setter
    def header(self, value):
        if self._check_fields:
            from igris_c_sdk.msg import Header
            assert \
                isinstance(value, Header), \
                "The 'header' field must be a sub message of type 'Header'"
        self._header = value

    @builtins.property
    def kinematic_modes(self):
        """Message field 'kinematic_modes'."""
        return self._kinematic_modes

    @kinematic_modes.setter
    def kinematic_modes(self, value):
        if self._check_fields:
            if isinstance(value, numpy.ndarray):
                assert value.dtype == numpy.uint8, \
                    "The 'kinematic_modes' numpy.ndarray() must have the dtype of 'numpy.uint8'"
                assert value.size == 5, \
                    "The 'kinematic_modes' numpy.ndarray() must have a size of 5"
                self._kinematic_modes = value
                return
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 len(value) == 5 and
                 all(isinstance(v, int) for v in value) and
                 all(val >= 0 and val < 256 for val in value)), \
                "The 'kinematic_modes' field must be a set or sequence with length 5 and each value of type 'int' and each unsigned integer in [0, 255]"
        self._kinematic_modes = numpy.array(value, dtype=numpy.uint8)

    @builtins.property
    def motors(self):
        """Message field 'motors'."""
        return self._motors

    @motors.setter
    def motors(self, value):
        if self._check_fields:
            from igris_c_sdk.msg import MotorCmd
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 len(value) == 31 and
                 all(isinstance(v, MotorCmd) for v in value) and
                 True), \
                "The 'motors' field must be a set or sequence with length 31 and each value of type 'MotorCmd'"
        self._motors = value
